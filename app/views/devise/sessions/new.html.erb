<div class="masthead-user">
  <div class="container">
      <div class="row justify-content-center">
        <div class="col-sm-12 col-lg-6">
          <div class="background-card mt-5">
            <h2>Log in</h2>
            <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="form-inputs">
                <%= f.input :email,
                            required: false,
                            autofocus: true,
                            input_html: { autocomplete: "email" } %>
                <%= f.input :password,
                            required: false,
                            input_html: { autocomplete: "current-password" } %>
                <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
                <div class="center-buttons">
                  <%= f.button :submit, "Log in", class: "btn btn-primary mb-2" %>
                    <div class="mb-4 mt-0">
                      <%= link_to "Forgot your password?", new_password_path(resource_name) %>
                    </div>
                      <p class="mb-2">Don't have an account yet?</p>
                        <%= link_to new_registration_path(resource_name) do %>
                        <button type="button" class="btn btn-outline-primary mt-0">Register</button>
                        <% end %>
                      <% end %>
                </div>
              </div>
              <div class="center-buttons mt-4">
                <%= link_to "Back", :back %>
              </div>
            </div>
         </div>
        </div>
      </div>
  </div>
</div>










