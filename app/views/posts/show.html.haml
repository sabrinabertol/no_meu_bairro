#neighbourhood_post_content
%h1= @post.title
%p= @post.content

= link_to "Edit", edit_neighbourhood_post_path(@post)
= link_to "Delete", neighbourhood_post_path(@post), method: :delete, data: { confirm: "Are you sure you want to do this ?" }
= link_to "Home", root_path
