<%= simple_form_for [@neighbourhood, @post] do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <div class="center-buttons">
    <%= f.button :submit, class: "btn btn-primary mt-2" %>
  </div>
<% end %>
