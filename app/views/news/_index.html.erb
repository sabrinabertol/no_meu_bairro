
<div class="container">
  <div class="mt-4">
    <h1 News </h1>
  </div>
  
  <% @news.each do |new| %>
      <div class="card-product mb-3">
        <%= image_tag new.photourl %>
          <div class="card-product-infos">
            <h2> <%= new.title %> </h2>
              <a href=<%= new.url %> target="_blank">info +</a> 
          </div>
      </div>
  <% end %>
</div>

      